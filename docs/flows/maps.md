<!-- GENERATED by apps/mobile/scripts/gen-flow-map.mjs — DO NOT EDIT BY HAND.
     Regenerate with `pnpm --filter @kavtsya/mobile flows:map`. -->

# User-flow maps

Derived from the Expo Router tree (`apps/mobile/src/app`) and the app's real
navigation call-sites. The three Role Modes (ADR 0015) are the surfaces
`index` dispatches to by live account facts — active Зміна → Scanner, else a
CafeOwner → CafeOwner, else Customer.

## Customer

```mermaid
flowchart TD
  root["Home<br/>(Mode dispatcher)"]:::screen
  settings["Settings<br/>(Налаштування)"]:::screen
  shift_request["Стати баристою<br/>(shift/request)"]:::screen
  signin["Вхід / Реєстрація<br/>(sign-in)"]:::auth
  customer["Customer Mode<br/>(Зернятка + QR)"]:::mode
  signin -- "вхід / реєстрація" --> root
  root -- "інакше" --> customer
  root -- "вихід" --> signin
  settings --> shift_request
  settings -- "вихід" --> signin
  customer -- "⚙" --> settings
  classDef auth fill:#fde68a,stroke:#b45309,color:#1c1917;
  classDef mode fill:#c7d2fe,stroke:#4338ca,color:#1c1917;
  classDef screen fill:#e7e5e4,stroke:#78716c,color:#1c1917;
```

## CafeOwner

```mermaid
flowchart TD
  root["Home<br/>(Mode dispatcher)"]:::screen
  owner_cafeId["Програма кав'ярні<br/>(owner/[cafeId])"]:::screen
  owner_analytics["Аналітика<br/>(owner/analytics)"]:::screen
  owner_campaigns["Розсилки<br/>(owner/campaigns)"]:::screen
  owner_roster["Ростер бариста<br/>(owner/roster)"]:::screen
  owner_scan["Сканувати QR клієнта<br/>(owner/scan)"]:::screen
  owner_shifts["Зміни<br/>(owner/shifts)"]:::screen
  settings["Settings<br/>(Налаштування)"]:::screen
  shift_request["Стати баристою<br/>(shift/request)"]:::screen
  signin["Вхід / Реєстрація<br/>(sign-in)"]:::auth
  owner["CafeOwner Mode<br/>(Режим Кавовара)"]:::mode
  signin -- "вхід / реєстрація" --> root
  root -- "власник кав'ярні" --> owner
  root -- "вихід" --> signin
  settings --> shift_request
  settings -- "вихід" --> signin
  owner --> owner_scan
  owner --> owner_cafeId
  owner --> owner_shifts
  owner --> owner_roster
  owner --> owner_campaigns
  owner --> owner_analytics
  owner -- "⚙" --> settings
  classDef auth fill:#fde68a,stroke:#b45309,color:#1c1917;
  classDef mode fill:#c7d2fe,stroke:#4338ca,color:#1c1917;
  classDef screen fill:#e7e5e4,stroke:#78716c,color:#1c1917;
```

## Scanner (Barista on shift)

```mermaid
flowchart TD
  root["Home<br/>(Mode dispatcher)"]:::screen
  signin["Вхід / Реєстрація<br/>(sign-in)"]:::auth
  scanner["Scanner Mode<br/>(Зміна — кіоск)"]:::mode
  signin -- "вхід / реєстрація" --> root
  root -- "активна Зміна" --> scanner
  root -- "вихід" --> signin
  classDef auth fill:#fde68a,stroke:#b45309,color:#1c1917;
  classDef mode fill:#c7d2fe,stroke:#4338ca,color:#1c1917;
  classDef screen fill:#e7e5e4,stroke:#78716c,color:#1c1917;
```

## Combined

```mermaid
flowchart TD
  root["Home<br/>(Mode dispatcher)"]:::screen
  owner_cafeId["Програма кав'ярні<br/>(owner/[cafeId])"]:::screen
  owner_analytics["Аналітика<br/>(owner/analytics)"]:::screen
  owner_campaigns["Розсилки<br/>(owner/campaigns)"]:::screen
  owner_roster["Ростер бариста<br/>(owner/roster)"]:::screen
  owner_scan["Сканувати QR клієнта<br/>(owner/scan)"]:::screen
  owner_shifts["Зміни<br/>(owner/shifts)"]:::screen
  settings["Settings<br/>(Налаштування)"]:::screen
  shift_request["Стати баристою<br/>(shift/request)"]:::screen
  signin["Вхід / Реєстрація<br/>(sign-in)"]:::auth
  scanner["Scanner Mode<br/>(Зміна — кіоск)"]:::mode
  owner["CafeOwner Mode<br/>(Режим Кавовара)"]:::mode
  customer["Customer Mode<br/>(Зернятка + QR)"]:::mode
  signin -- "вхід / реєстрація" --> root
  root -- "активна Зміна" --> scanner
  root -- "власник кав'ярні" --> owner
  root -- "інакше" --> customer
  root -- "вихід" --> signin
  settings --> shift_request
  settings -- "вихід" --> signin
  customer -- "⚙" --> settings
  owner --> owner_scan
  owner --> owner_cafeId
  owner --> owner_shifts
  owner --> owner_roster
  owner --> owner_campaigns
  owner --> owner_analytics
  owner -- "⚙" --> settings
  classDef auth fill:#fde68a,stroke:#b45309,color:#1c1917;
  classDef mode fill:#c7d2fe,stroke:#4338ca,color:#1c1917;
  classDef screen fill:#e7e5e4,stroke:#78716c,color:#1c1917;
```

