# Kavtsya — Customer Core Loop redesign · Engineering handoff
Date: 2026-07-20 · Source canvas: `Customer Core Loop.dc.html` (screens marked «✓ ОБРАНО»)
All values are semantic tokens from `apps/mobile/src/theme/design-tokens.json`. No new hexes.

## Approved screens — ALL screens on the canvas are approved (turns 1–9)
- **1b home dark, 1c loading, 1e empty, 1l Ворожка reward-ready** — approved as drawn; specs for each are described inline above/below (1b = 1a structure on `semantic.dark.*`; 1c reserved QR square on `primary-surface` + «Готуємо твій код…»; 1e Берегиня empty state «Перше зернятко чекає»; 1l reward-ready fortune: beanrow on `primary-surface` + `primary` border, «★ ВИНАГОРОДА ГОТОВА», CTA «Отримати Винагороду» + ghost «Пізніше»).
- **1a — Customer Mode home (light).** No brand logo on home (logo lives on sign-in + splash only). Header row = greeting «Вітаємо, {name}!» (Cormorant 600, ~23–24) + settings line-icon (24-grid, 1.6px stroke) in a 44pt target. Raw email removed from home → Settings. QR card: `surface`, `border`, `radius-lg`; title «Твій код учасника» (Manrope 600 16); under QR a live-token cue: slow ring (`primary`, decorative/aria-hidden) + «Код живий — оновлюється сам» (`text-muted`, 12); member code 24px tabular + «Не сканується? Продиктуй кавовару цей код». «МОЇ КАВ'ЯРНІ» section label prefixed with a 13px Берегиня mark in `primary` (aria-hidden). Café row: name + reward name always visible (`text-secondary`), caption «4 зернятка · ще 1 до Винагороди» (`text-muted`), bean-shaped progress dots — filled = `primary` fill + espresso crease (`primary-foreground`, .55), empty = dashed `border-strong` outline. Dark theme = same structure on `semantic.dark.*`; QR on a white plate (**proposed token: `qr-plate`**). QR has NO center logo (decision: rejected).
- **1d — Home error state.** Hierarchy inverted: member code is the hero (28px tabular, nowrap, on `primary-surface` card) with «Продиктуй кавовару — зернятко зарахується так само»; failure is a quiet strip `danger-surface` + `danger` icon+text («Немає з'єднання — QR тимчасово недоступний», live region); retry = secondary button, `border-strong`, ≥44pt.
- **1f — Redemption ready (light).** Café card elevated: 1.5px `primary` border, gold glow shadow (`primary` @25%, **proposed `shadow-reward` token**), chip «★ Винагорода готова» (`primary` bg / `primary-foreground` text), full bean row, CTA «Як отримати» (`primary`, ≥44pt).
- **1g — Redemption sheet (light).** Bottom sheet `surface`, `radius-xl` top corners, scrim = foreground @45%. Handle, 18px Берегиня in `primary`, title «Винагорода готова» (Cormorant 600 24), centered subtitle «Безкоштовний напій · Кав'ярня «Демо»» (`text-secondary`), same single QR (one-scan model, no second scan), copy states ledger math: «Спишеться 10 зернят, решта збережеться», «Пізніше» secondary ≥44pt. (Dark sheet variant was reviewed and dropped.)
- **1h — Redemption success (light).** Banner on `success-surface`: check glyph stroke `success`, title «Винагороду отримано!» in `success` (NOT `success-foreground` — that token pairs with solid green fill only; on the pale surface it fails contrast), detail «Безкоштовний напій · −10 зернят» in `text-secondary`; announced as live region. Café card below narrates the subtraction: «3 зернятка збережено · ще 7 до наступної» + bean row 3/10.
- **1k — Ворожка reveal (dark, always).** Decision: Ворожка always renders dark regardless of system theme (option A). `semantic.dark.background` (indigo), gold radial glow, kicker «ВОРОЖКА» 16px/700, letter-spacing 3.5, `primary`, flanked by two 15px Берегиня marks; café name `text-muted`; cup illustration (from the approved states exploration, brand hexes = neutral-100/300/900 + primary-400) with **animated grounds swirling inside the cup surface** — dots+arcs in espresso, elliptical orbit (scale(1,0.29) + 5s rotate loop; the 5s = `vorozhka` motion token); fortune in Cormorant 600 24, guillemets, lead phrase in `primary`; beanrow card on dark `surface` with «+1 Зернятко · ще N до Винагороди»; CTA «Дякую» (`primary`/`primary-foreground`, 48pt). Text appears instantly — no shimmer/typewriter. Reduced-motion: freeze swirl, plain 400ms fade.

## Deleted during review
- 1i Redemption sheet dark; 1j Ворожка brewing screen (0–5s pre-screen cut — the reveal screen itself carries the in-cup animation).

## Cross-cutting decisions
- Copy: «ти» form everywhere (e.g. «Покажи», «Продиктуй»); glossary terms verbatim (Зернятко, Ворожка, Винагорода, «покупка»). No emoji; Берегиня ornament instead, always small + `primary` + aria-hidden.
- Beans progress = coffee-bean shape (oval + curved crease), not circles; shared across home / redemption / Ворожка.
- Proposed token additions (do not hardcode): `qr-plate` (white QR surface in dark), `shadow-reward` (gold glow).
- QR: generate at EC level H if any future center mark returns; live-token cue text is part of the anti-screenshot posture (ADR 0006).
- A11y floor: all CTAs ≥44pt; status = icon + text, never color alone; error/success = live regions; decorative SVGs aria-hidden; muted text ≥13px.

## Turn 2 — CafeOwner scan → issue/confirm + Scanner Mode (ALL approved: 2a–2g)
PNGs 11–17. Applies to `scan-workstation.tsx` (owner scan + shift scanner share it; only header/exit differ).

- **2a/2b — Scanning (light/dark).** Logo removed; permanent role header: caption «РЕЖИМ КАВОВАРА» (`text-muted` 11/700, ls 2) + café name (`font-display` 600 22) — role & credit-target always visible. Viewfinder: full-width square well, `radius-lg`, bg `secondary` (light) / near-black (dark); gold corner brackets + horizontal scan line in `primary` (decorative, aria-hidden); in-frame hint «Наведи камеру на QR-код клієнта» in neutral-100 on indigo, kept BELOW the brackets (brackets stop ~64px above bottom edge). Member-code field inline below (48pt, placeholder «Або введи код клієнта»); «Нарахувати за кодом» appears once typing starts (existing logic). Back button secondary 44pt.
- **2c — Issued.** Success strip `success-surface`: check glyph `success`, title «Зернятко зараховано» in `success`, subtitle «{name} · Ворожка вже на її екрані» in `text-secondary`. Card: «Зернятка N з T» + bean row. Single primary CTA «Сканувати ще» (`primary`, 48pt). **Decision: the fortune text is no longer rendered on the barista's screen** (ritual stays on the Customer's device, screen 1k) — render-only change, response payload untouched.
- **2d — Issued + Reward ready.** Compact success strip, then the confirm card is the screen: `surface`, 1.5px `primary` border, `shadow-reward` glow; kicker «★ ВИНАГОРОДА ГОТОВА · 11 З 10» in `link`; reward name in `font-display` 22; math stated pre-confirm: «Підтверди — спишеться 10 зернят, 1 збережеться»; CTA «Видати Винагороду» primary 48pt inside the card; «Сканувати ще» demoted to secondary at bottom.
- **2e — Redeemed.** `primary-surface` card + `primary` border, Берегиня 22px in `link`, «Винагороду видано», reward + name, ledger line «−10 зернят · залишок 1 з 10». Banked balance (≥2×T): re-offer «Видати ще одну» (same confirm). Primary «Сканувати ще».
- **2f — Rejected (#50 taxonomy).** `danger-surface` strip: alert glyph `danger`, bold title + recovery instruction in `text-secondary`. Copy per case: застарілий код → «Код застарів / Попроси клієнта оновити екран із QR — код живе 90 секунд»; уже використано → «Код уже використано / Попроси клієнта оновити екран»; не знайдено → «Клієнта не знайдено / Перевір код і спробуй ще»; самоскан → «Не можна сканувати власний код». Primary «Сканувати ще». Live region.
- **2g — Scanner Mode («Зміна»).** Identical workstation; frame differs only: indigo banner (`secondary` bg, `radius-md`): «ЗМІНА · КАВ'ЯРНЯ «ДЕМО»» (neutral-100, 12/700 ls2) + barista name (neutral-100, 16/600, NO expiry time — decision), Берегиня 16px `primary` right; single exit «Завершити зміну» (secondary button) — no settings, no nav (kiosk floor, ADR 0015).
- Status-bar time row present on all screens of the flow. All CTAs ≥44pt; status = glyph+text; outcome switches announced.

## Turn 3 — CafeOwner home + program config (3a–3f, PNGs 18–23)
- **3a/3d — Owner home (light/dark).** Logo removed; role header «РЕЖИМ КАВОВАРА» + café name + settings icon (44pt). Stat card (people glyph + «N клієнтів повернулися за 30 днів») on `surface`. Hero: «Сканувати QR клієнта» `primary` 56pt with viewfinder glyph. Menu = two grouped lists (52pt rows, line icons, chevrons): management (Програма лояльності / Зміни / Ростер бариста) and growth (Розсилка / Аналітика) with visible **PRO** pill — `secondary`+neutral-100 in light, `primary`+`primary-foreground` in dark; row still opens the pitch (ADR 0011). Footer: «Твій особистий профіль — у Налаштуваннях». Dark = identical structure on semantic.dark.
- **3b/3e — Program config (light/dark).** Threshold = ±stepper (44pt round buttons: − secondary outline, + `primary`; value 34px tabular) + live bean-row preview captioned «Так це побачить клієнт». Rewards = radio cards (48pt): selected `primary-surface` + 1.5px `primary` + filled radio in `link`; ₴/% param inline on discount rows. Save `primary` 48pt. Validation = danger strip under the offending field (pattern of 2f).
- **3c/3f — Saved (light/dark).** Dedicated confirmation: strip on `success-surface` with 1px `success` border, role="status": solid `success` circle badge + `success-foreground` check; title «Збережено» in `text-body`, body «Клієнти вже бачать нові умови» in `text-secondary` (no colored-sentence text). Summary card «Безкоштовний напій · 10 зернят» + snapshot rule note. Primary button becomes «Готово» (exit).

## Dark variants (approved, PNGs 24–34)
Every flow screen now exists in both themes: 1m loading / 1n error / 1o empty, 1p ready / 1q sheet / 1r success, 2h issued / 2i reward-ready / 2j redeemed / 2k rejected / 2l scanner shift. Same structure, semantic.dark tokens. Specifics: QR always renders on a white plate with espresso modules in dark (scanability); dark sheet scrim = rgba(27,20,44,.55); 2l shift banner in dark uses `surface`+`border` with `text-body` (NOT --secondary, which is lavender in dark); viewfinder well in dark = near-black.

## Turn 4 — Settings + Analytics (4a–4e, ALL approved, PNGs 35–39)
- **4a/4c — Settings, Customer (light/dark).** No logo, no gear glyph on the screen itself; header = back icon (44pt) + «Налаштування» (font-display 600 24). Identity card first: avatar circle `primary-surface` + serif initial in `link` (light)/`primary` (dark), name 15/700, email in `text-muted` (email lives HERE, not on home). Sections (11px/700 ls1.5 `text-muted` labels): СПОВІЩЕННЯ — toggle row «Новини від кав'ярень» + caption «Push лише від кав'ярень, де ти буваєш» (ти-form); toggle on = `primary` track + white knob, off = `border-strong` outline track, announced увімкнено/вимкнено. РОЛІ — two rows, progressive disclosure (no inline form): «Стати Кавоваром» (storefront glyph → register-café subscreen), «Приєднатися до кав'ярні» (viewfinder glyph → poster-scan shift/request); helper caption below. АКАУНТ — «Вийти» neutral row; «Видалити акаунт» (#81) in `danger` text + trash glyph, confirm dialog behind it. Rows 52pt.
- **4b — Settings, CafeOwner (light).** Same anatomy plus: КАВОВАР badge (`secondary`+neutral-100) on identity card; МОЇ КАВ'ЯРНІ section (café rows + «Зареєструвати ще одну»); РОЛІ holds the ADR-0015 excursion «Мій профіль клієнта — Твій QR і зернятка» (the only door to Customer Mode). Deletion row omitted where an owned Café requires transfer flow — confirm with product.
- **4d/4e — Analytics, Pro (light/dark).** Role header «АНАЛІТИКА · PRO» + café name + top back icon (no logo/gear, no bottom «Назад»). Segmented 7/30 pill control (selected `primary`+`primary-foreground`, radio-group semantics). Stat trio cards (26px tabular): Активні / Нові / Повернулися — «Повернулися» accented `primary-surface`+`primary` border (the loyalty metric); one-line glossary below defines all three. Peak-hours card: insight promoted to serif headline «Найбільше зернят — близько 19:00» (no «за Києвом» qualifier — dropped per review); 20 bars, non-peak `primary-surface` (light)/`surface-raised` (dark — NOT secondary-surface, which equals surface in dark), peak solid `primary`; hairline baseline + hour labels 06–24; accessible chart summary text instead of per-bar labels. States: loading = skeleton bars 20% opacity; empty = Берегиня + «Ще замало даних — графіки з'являться після перших сканів»; error = danger strip + retry (pattern 1d).

## Turn 5 — Role subscreens · Pro pitch · Ростер · Зміни (5a–5l, ALL approved, PNGs 40–51)
Light screens 5a–5f; dark twins 5g–5l are structurally identical on `semantic.dark.*` (`.dark` remap, no per-screen overrides) — owner-admin dark rule from turn 3 applies. Specifics below are the light spec.
- **5d — Стати Кавоваром (register café).** Own subscreen (opened from Settings РОЛІ row — never an inline form). Back + serif title 24. Two fields only: НАЗВА КАВ'ЯРНІ (focused: 1.5px `primary` border + blinking caret; reduced-motion → static) and МІСТО (placeholder «Наприклад, Львів» in `text-muted`; labels persist above values — placeholder never doubles as label). Promise card `primary-surface` + 18px Берегиня in `primary`: «Після реєстрації з'явиться Режим Кавовара: програма зернят, постер для каси, ростер бариста». CTA «Зареєструвати кав'ярню» `primary` 48pt; footer defuses price: «Безкоштовно. Pro-функції — окремо, коли захочеш».
- **5e — Приєднатися до кав'ярні (barista).** Camera-first, mirrors the turn-2 Scanner viewfinder: square plate `semantic.dark.background` + `radius-lg` (in dark theme add hairline `border`), gold corner brackets in `primary` (aria-hidden), in-plate hint «Наведи камеру на код постера» in `semantic.dark.text-secondary`. Trust rule surfaced BEFORE scanning (`primary-surface` card): «Доступ підтверджує Кавовар — твій запит з'явиться в нього в Ростері» (ADR 0013). Fallback «Ввести код вручну» = outline `border-strong` 48pt.
- **5f — Запит надіслано (waiting state).** Explicit, never a toast: clock glyph in `primary` on 76px `primary-surface` circle, serif title 26 «Запит надіслано», body names café + what happens next («сповістимо, щойно підтвердить»); icon+text, never spinner alone. «Добре» primary 48pt + quiet «Скасувати запит» 40pt. Pairs 1:1 with the owner-side request card in 5b.
- **5a — Розсилка Pro pitch.** Role header «РОЗСИЛКА · PRO» + café name + top back icon. Offer card `surface` + 1.5px `primary` border, Берегиня 22px `primary`, serif promise 22 «Розкажи постійним про нове — одним повідомленням», three `success` checkmarks (icon+text). Price strip `primary-surface` (16px above card / 12px below): «Pro — від ₴390/міс · 14 днів безкоштовно» — the numbers ARE the v1 upgrade path (ADR 0011): CTA «Написати нам у Telegram» `primary` 48pt + mail fallback caption. No emoji, no in-app payment.
- **5b — Ростер бариста.** Poster-code card first: label КОД ПОСТЕРА, 22px tabular code, trust caption «доступ дає твоє підтвердження, не сам код» (ADR 0013). ЗАПИТИ section = loudest element: count badge `primary`/`primary-foreground` in the header, request card 1.5px `primary` border, Підтвердити (`primary`) / Відхилити (`border-strong` outline) 44pt pair. БАРИСТА list: avatar (`primary-surface` + serif initial in `link`), live shift status = green dot + «на зміні» text (never dot alone), remove = 44pt icon button, aria-label «Прибрати з ростеру» + confirm dialog.
- **5c — Зміни.** Intro caption explains self-start («бариста сканують постер самі» → code lives in Ростер). НА ЗМІНІ row: avatar, name, green dot + «з 08:12», scan counter «14 сканів» on its OWN line in `text-muted`, owner-side «Завершити» outline 40pt (ADR 0013 owner-end). ЗАВЕРШЕНІ СЬОГОДНІ rows: time span + scan total. Footer: «Зміна закривається сама після ліміту безпеки». NO manual «Оновити» buttons anywhere — lists self-refresh (pull-to-refresh); a refresh button is a debug artifact.
- Cross-cutting: role-header pattern (kicker + café name + top back), no logo, «ти» copy, role names verbatim (Кавовар, бариста), all CTAs ≥44pt.

## Turn 6 — Аналітика Pro pitch · Confirm dialogs (6a–6h, ALL approved, PNGs 52–59)
- **6a/6h — Аналітика pitch (light/dark).** Twin of 5a — identical anatomy (role header «АНАЛІТИКА · PRO» + café + top back → offer card → price strip → Telegram CTA + mail fallback); only promise/proof differ. Serif promise «Побач, коли твоя кав'ярня жива»; inside the card a 12-bar skeleton chart preview (aria-hidden): non-peak `primary-surface` (light) / `surface-raised` (dark — turn-4 chart rule; `primary-surface` ≡ card surface in dark), 3 peak bars solid `primary`. Checks cross-sell Розсилка exactly as 5a cross-sells Аналітика. Price strip «Pro — від ₴390/міс · 14 днів безкоштовно» (ADR 0011).
- **Confirm dialogs — one shared anatomy** (replaces OS alerts; brand type + «ти» voice): scrim rgba(27,20,44,.5) light / rgba(10,7,20,.6) dark; card `surface` + `radius-xl`, role="alertdialog", focus trapped; context chip → serif title 23 → body in `text-secondary` → stacked full-width 48pt buttons, confirm on top. Verbs on buttons, never «Так/Ні» alone. Esc/scrim-tap = cancel everywhere EXCEPT delete-account (explicit button only).
- **6b/6e — Видалити акаунт (light/dark).** Irreversible → warning glyph on `danger-surface` chip; consequences named concretely (зернятка, історія Ворожки — назавжди); confirm «Так, видалити назавжди» = solid `danger` (dark fg = `semantic.dark.background`, NOT #fff — dark `danger` is salmon, white fails AA); safe action labeled «Залишитися» (the pair must not read as two exits). Initial focus on the SAFE action.
- **6c/6f — Прибрати з ростеру.** Reversible → neutral: avatar chip names the person; body states all three consequences (no more shifts, active shift ends, can re-request via poster); confirm `primary`. Initial focus on confirm.
- **6d/6g — Завершити зміну.** Same anatomy as 6c; body = shift facts («З 08:12 · 14 сканів») + immediate effect («Сканер на його пристрої закриється одразу»).
- **Dark avatar fix (retro-applied to ALL dark screens, turns 4–6):** avatar circles on `primary-surface` are invisible in dark (token ≈ surface) → every dark avatar gains a 1px `primary` ring. Light screens unchanged.

## Turn 7 — Owner deletion → передати / закрити кав'ярню (7a–7h, ALL approved, PNGs 60–67)
Resolves the turn-4 open item. Principle: deletion is never blocked without naming the exit. Dark twins 7e–7h = `.dark` remap + turn-6 rules (deep scrim rgba(10,7,20,.6), avatar `primary` ring, danger confirm fg = `semantic.dark.background`).
- **7a/7e — Спершу — кав'ярня (blocked dialog).** Owner taps «Видалити акаунт» → dialog (turn-6 anatomy, storefront glyph on `primary-surface` chip): names why («Ти Кавовар «Демо»») and both exits — «Передати кав'ярню» primary, «Закрити назавжди» danger OUTLINE (demoted), quiet «Скасувати». Never silently hide the deletion row.
- **7b/7f — Передати кав'ярню.** Subscreen: intro promises continuity («програма, постер, ростер, історія — клієнти нічого не втратять»). З РОСТЕРУ radio rows first (22px ring `link` + filled dot; roster people have trust history), АБО ЗА EMAIL field (persistent-label pattern) for outsiders. Берегиня card = safety rule: nothing moves until the recipient ACCEPTS; CTA «Надіслати запрошення» 48pt.
- **7c/7g — Запрошення надіслано.** Exact 5f waiting anatomy (clock on `primary-surface` circle, serif 26, what-happens-next body, «Добре» + quiet «Скасувати запрошення») — one waiting pattern app-wide. Café stays the owner's until acceptance; then deletion unblocks and standard 6b applies.
- **7d/7h — Закрити «Демо» назавжди?** Danger confirm, consequences QUANTIFIED and honest: «37 клієнтів втратять свої зернятка — і ми їм про це повідомимо» + shifts end + poster dies + «це незворотно». Confirm «Так, закрити назавжди» solid `danger`; safe action is NOT «Скасувати» but «Краще передати кав'ярню» — the dialog argues for the better exit. Focus starts on the safe action; Esc/scrim-tap does NOT confirm-close (explicit buttons only, per 6b rule).
- Not designed (flagged for a future turn): recipient-side accept screen (Марко's view of the transfer invite).

## Turn 8 — Transfer accept, recipient side (8a–8f, ALL approved, PNGs 68–73)
Dark twins 8d–8f = `.dark` remap + standing dark rules (deep scrim, avatar `primary` ring).
- **8a/8d — Тобі передають кав'ярню (invite).** Informed consent, never a bare push button. Offer card (`surface` + 1.5px `primary`, pattern of 5a/6a): inviter chip (Олена avatar + «запрошує тебе стати Кавоваром»), serif café name, then 2 `success` checks (what transfers: програма/постер/ростер/історія; 37 клієнтів і зернятка залишаються) + 1 `warning` line (what it obliges: умови програми, Винагороди) — icon+text. Берегиня card removes time pressure: «Не поспішай — Олена лишається Кавоваром, доки ти не приймеш». «Прийняти кав'ярню» primary 48pt; «Відхилити» quiet 44pt.
- **8b/8e — Тепер ти Кавовар (accepted).** Mode-unlock moment: waiting-state anatomy but Берегиня (brand mark, not checkmark) on the `primary-surface` circle; body restates inheritance (37 клієнтів із зернятками); CTA «До Режиму Кавовара» + quiet «Пізніше» (бариста may be mid-shift). Live region.
- **8c/8f — Відхилити запрошення? (dialog).** 6c anatomy; honest consequence (Олена дізнається) + recovery path (зможе надіслати ще раз) — guilt-free, so confirm stays neutral `primary`, NOT danger. Focus on confirm; Esc/scrim = cancel.

## Turn 9 — Експорт бази клієнтів (9a–9f, ALL approved, PNGs 74–79)
New feature. Dark twins 9d–9f = `.dark` remap + standing dark rules (avatar `primary` ring; КАВОВАР badge in dark = `primary`+`primary-foreground`, not `secondary`; the Берегиня `primary-surface` card gains a hairline `border` in dark since `primary-surface` ≈ surface there).
- **Privacy principle.** Export contains the ACTIVITY picture only — names, member codes, зернятка, Винагороди, first/last visit. NO phones/emails: clients gave contacts to Кавця (push), not to the café; contacting them happens via Розсилка. The exclusion is stated on-screen with the reason and the alternative — never silently enforced.
- **Free, not Pro-gated** — it's the owner's own data (deliberate decision).
- **9a/9d — Entry.** Owner Settings (4b) gains a **ДАНІ** section between МОЇ КАВ'ЯРНІ and РОЛІ (export is data, not a café-management verb). Row: download glyph (24-grid, 1.6px stroke), title «Експорт бази клієнтів», caption «CSV на твій email — зернятка й візити» (pre-answers format + delivery), chevron, 52pt.
- **9b/9e — Export screen.** Role header «ЕКСПОРТ БАЗИ» + café name + top back icon. Manifest-first card (`surface`+`border`, `radius-lg`): count headline «37 клієнтів · CSV», two included groups as `success` checks, exclusion as an ✕ glyph in `text-muted` + `text-secondary` copy — informational, NOT danger (nothing failed): «Без телефонів та email — клієнти лишили їх Кавці, а не кав'ярні. Написати їм можна через Розсилку». Берегиня card `primary-surface` names the delivery address (email in bold `text-body`). CTA «Надіслати CSV на email» `primary` 48pt; footer «Останній експорт: 12 липня» makes repeat taps self-explaining. Delivery by email, never a file on the phone (spreadsheets are read on a laptop).
- **9c/9f — Надіслано.** Standard waiting/success anatomy (5f/8b family): envelope glyph `success` on 76px `success-surface` circle, serif title 26 «Експорт надіслано», body restates count + address + spam-folder hint, «Готово» primary 48pt. Live region.

## Next scope
None. Feature-complete (turns 1–9). Possible future: opt-in contact sharing (requires a customer-side consent toggle — design both sides). Token additions (`qr-plate`, `shadow.reward`, `scrim`) are specced as a ready-to-paste patch in `handoff/design-tokens-additions.md` — apply to `mobile/src/theme/design-tokens.json`.
