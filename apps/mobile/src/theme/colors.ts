/** Kavtsya design tokens — the single source of truth for the palette. */
export const colors = {
  background: "#fffaf3",
  surface: "#ffffff",
  brand: "#3b2417",
  onBrand: "#fffaf3",
  border: "#d8c9bc",
  muted: "#9b8b7e",
  accent: "#7a5c45",
  chipActiveBackground: "#f0e6db",
  error: "#b00020",
} as const;

/** Default corner radius for cards, inputs, buttons, and chips. */
export const radius = 8;
